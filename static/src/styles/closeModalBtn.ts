import styled from 'styled-components'
import { PropsWithTheme } from '../types/styles'


const Btn = styled.div<PropsWithTheme>`
  position: absolute;
  top: 1rem;
  right: 1rem;
  padding: 1rem;
`

export default Btn